import React from 'react';

const Error = () => {
  return (
    <h1
      style={{
        color: 'red',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '50vh',
      }}>
      Такой страницы не существует!
    </h1>
  );
};

export default Error;
